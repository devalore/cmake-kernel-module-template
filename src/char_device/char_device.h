#ifndef _CHAR_DEVICE_H_
#define _CHAR_DEVICE_H_

// Install the example character device
int char_device_install(void);

// Uninstall the example character device
void char_device_remove(void);

#endif
